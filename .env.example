# Server
SERVER_PORT=8080

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=order_management
DB_SSL_MODE=disable

# For migrations: full DSN
# DATABASE_URL=postgres://user:password@localhost:5432/order_management?sslmode=disable
